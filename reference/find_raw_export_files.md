# Finds raw exported files ending with CSV

Finds raw exported files ending with CSV

## Usage

``` r
find_raw_export_files(raw_export_dir)
```

## Arguments

- raw_export_dir:

  Path to the directory where raw data is located.
