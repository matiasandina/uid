# Helper to pluck the temperature and activity datasets separately we can consider including this instead of the bind_rows(.id = "variable") the con of this is handling multiple files

Helper to pluck the temperature and activity datasets separately we can
consider including this instead of the bind_rows(.id = "variable") the
con of this is handling multiple files

## Usage

``` r
pluck_bind_temp_activity(clean_list, nm)
```
