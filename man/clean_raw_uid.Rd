% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_temperature.R
\name{clean_raw_uid}
\alias{clean_raw_uid}
\title{Wrapper to clean UID temperature CSV: read, flag outliers, downsample, and plot}
\usage{
clean_raw_uid(
  filepath,
  n = 1,
  precision = "minute",
  outlier_threshold_celsius = 1,
  output_dir = "temperature/data",
  plot = TRUE,
  flicker_correction = TRUE,
  flicker_dominant_two_thr = 0.65,
  flicker_alt_rate_thr = 0.4,
  flicker_contiguous_thr = 0.65
)
}
\arguments{
\item{filepath}{Path to a raw UID CSV.}

\item{n}{Downsampling interval size (default = 1) applied to both temperature
and activity data.}

\item{precision}{Time unit for downsampling (default = "minute").}

\item{outlier_threshold_celsius}{Temperature difference threshold to flag outliers.}

\item{output_dir}{Directory to save diagnostic plots.}

\item{plot}{boolean to indicate whether to plot the process of outlier removal and downsampling}

\item{flicker_correction}{Whether to apply activity flicker correction before quantification.}

\item{flicker_dominant_two_thr}{Threshold for dominant two-zone occupancy fraction.}

\item{flicker_alt_rate_thr}{Threshold for ABAB alternation rate within minute bins.}

\item{flicker_contiguous_thr}{Threshold for fraction of contiguous transitions within minute bins.}
}
\value{
A list with cleaned, downsampled \code{temperature} and \code{activity} data frames.
}
\description{
The cleaning workflow assumes that each \code{rfid} belongs to a single
\code{matrix_name} within a session. If the same \code{rfid} is detected on more than
one matrix, the function prints a summary table with \code{session_name},
\code{rfid}, \code{matrix_name}, \code{n}, \verb{\%}, \code{min_dt}, and \code{max_dt}.
}
\details{
Low-percentage duplicate detections can happen when matrices are placed too
close to each other or when a cage is briefly placed on the wrong platform.
In those cases, the function asks for explicit confirmation before removing
rows from the matrix with the lower percentage of detections. If duplicate
measurements on the secondary matrix exceed 10\% of detections for that
\code{session_name}/\code{rfid} pair, the function aborts and requires manual review.
}
